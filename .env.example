# Database Configuration
DB_URL=jdbc:postgresql://localhost:5432/castelCSR_DB
DB_USERNAME=postgres
DB_PASSWORD=tu_contraseña_postgres_aqui

# JWT (Fase 2) - debe ser Base64 de 64 bytes (512 bits) para HS512
# Generar con: openssl rand -base64 64
JWT_SECRET=tu_jwt_secret_base64_de_64_bytes_aqui

# Email (para futuro)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=tu_email@gmail.com
MAIL_PASSWORD=tu_app_password_aqui